# 2026-06-13

## 博客模板内容清理

清除了 VuePress 脚手架残留的模板内容：

### 删除的文件/目录
- `src/demo/` — 6个功能演示页面（全模板占位）
- `src/intro.md` — 模板介绍页
- `src/.vuepress/public/logo.svg` / `logo.png` / `favicon.ico` — 模板 logo
- `src/.vuepress/public/assets/images/cover1-3.jpg` — 模板封面图
- `src/.vuepress/public/assets/icon/` — 全部模板图标

### 修改的配置
- **src/README.md**：删除 projects 占位条目、删除 heroImage、清理 footer 和模板说明
- **src/.vuepress/theme.ts**：清理 hostname→实际地址、repo→ZZ7012/TyrionBlogs、删除示例社交链接、删除 encrypt(demo)、删除 intro 引用
- **src/.vuepress/navbar/en.ts 和 zh.ts**：水果示例导航 → 简洁的 Home + Posts(children:structure) 结构
- **src/.vuepress/sidebar/en.ts 和 zh.ts**：删除 Demo/Slides/intro 条目 → 仅保留 Articles(children:structure)

## 编译报错修复
- **navbar**：`children: "structure"` 在 navbar 中不合法（仅 sidebar 支持），改为 `link: "/posts/"`
- 清除 `.vuepress/.temp` / `.cache` 缓存后重建成功

## 图片空格修复
- 写 `fix_image_spaces.py` 批量重命名 29 个含空格图片 → 短横线，更新 40 处 Markdown 引用
- `CANTp.md` 中 5 张图片不存在（Obsidian 源也缺失），标记为 "图片丢失，待补充"

## Obsidian → VuePress 同步脚本
- 创建 `sync_obsidian_to_vuepress.py`
- 处理：`![[...]]` wiki 链接 → `![](./resources/...)` Markdown 链接
- 图片文件名空格 → 短横线，自动去重
- 缺失图片 → 注释警告而非构建报错
- 自动补 `---\ntitle: xxx\n---` frontmatter
- 挂载到 `package.json`：`docs:build` 前自动执行 `docs:sync`
- 用法：在 Obsidian 编辑后，运行 `docs:sync` 或直接 `docs:build`（自动同步+构建）

## Dev Server
- `npx vuepress-vite dev src` → `http://localhost:8080/TyrionBlogs/`
